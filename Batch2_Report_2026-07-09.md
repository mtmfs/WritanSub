# WritanSub 批次 2 报告 — 测试基建 + 重排对齐 + 三项实施

- 日期：2026-07-09（同日先序工作见 `Review_Report_2026-07-09.md`）
- 范围：pytest 测试基建落地 → 批次 2 逐项对齐重排 → 计划内三项（T24/T01/T04）实施
- 本轮 4 个 commit，全部已推送 GitHub：

| Commit | 内容 | 规模 |
|--------|------|------|
| `29e20c4` | pytest 测试基建（38 例，全离线可重复） | 9 文件 +566 |
| `fe97c60` | T24 TIGER-DnR 默认单轨推理（预处理 ~3x 提速） | 3 文件 +100/−10 |
| `9c64de2` | T01 主修 参数改编辑即写（根除全零自锁） | 5 文件 +71/−12 |
| `31dc8be` | T04 输出命名三段式 + 终稿双轨 + 双语开关 | 6 文件 +109/−33 |

---

## 批次 2 对齐结果（12 项处置）

经逐项对齐（含一次计划模式全流程），批次 2 从原 10 项 + 回池 2 项重排为：

- **本轮实施**：T24（授权直接做）、T01 主修（需要改）、T04（命名方案三轮对齐后定稿）
- **等真实素材**：T29（数字转读音）→ A/B → T22（双模型阈值标定），用户提供数加加财经素材后启动
- **优先级 0 暂缓**：T09（设计已定：本 tab 无任务则取消按钮置灰，不做全局互斥）、T12、T13（网络依赖问题"用户自己的问题"）
- **池底**：T40（−2，无 N 卡场景不管）、T07（−1）、T08（−1）、T36（以后再说，详见下文"取消体验"）

## T24 — TIGER-DnR 单轨推理

三倍浪费在 Python 层而非模型内部：`separate_dnr` 对整段音频用 dialog/effect/music 三个独立子模型各跑一遍全长推理，流水线只消费人声。修复为按 `full_tracks` 裁剪子模型循环（`run_dnr_batch` 传 `save_intermediate`），vendor 与 Demucs 路径零改动。

验证：3 例桩模型单测；Hathaway 76s 真实重跑 `--denoise`，日志仅"人声(1/1)"，产出 SRT 与三轨版**逐字节全等**；全流水线 41s。

## T01 主修 — 参数保存改"编辑即写"

根因：closeEvent 用 `findChildren(ParamSpinBox)` 全窗口扁平收集写盘，align 页 6 键、transcribe 页 1 键与 pipeline 页 8 键全部重叠，遍历顺序决定谁赢，未初始化值可入盘并自锁。

修复比原计划（closeEvent 命名空间化）更深一层：`widgets.bind_pp_autosave` —— spinbox 关闭 keyboardTracking 后接 valueChanged，触发即 `load | {key: val}` 单键合并写盘；关窗收集整块删除。语义变为"多页同键 = 同一配置的多视图，最后编辑者赢"；**未触碰的 spinbox 永不写盘**，未初始化污染在机制上不可能再发生。

验证：3 例单测（单键写不丢他键 / 未触碰不写盘 / 双视图后编辑者赢）；离屏真 MainWindow 四轮行为验证（不碰不写、编辑落盘、多轮稳定、重载播种），配置路径重定向临时目录未触真实配置。

⚠️ T01 修复后 review 功能真正复活，T06 的标注错位从隐性变显性（交付前跑 `--review` 可能标错行），批次 3 消除。

## T04 — 输出命名新规（用户三轮对齐定稿）

| 产物 | 文件名 | 说明 |
|------|--------|------|
| 源语终稿 | `<base>.srt` | 流水线必产，**恒写、不被翻译覆盖**；且先于翻译阶段落盘，翻译中途取消也不丢日文轴 |
| 翻译终稿 | `<base>_<lang>.srt`（简体中文→`_chs`） | 开翻译时**与源语终稿并存**；内容默认双语，`--no-bilingual` 切单语 |
| whisper 原始 | `<base>_original_whisper-<模型>.srt` | `--keep-whisper-srt`；与终稿不再同名互相覆盖（T04 原病根除） |
| 对齐留档 | `<base>_aligned_<模型>.srt` | `--keep-aligned-srt` |
| 单步命令默认 | transcribe/align 同三段式；translate → `_<lang>.srt` | `-o` 显式路径不变 |
| review / 中间音轨 | 不变 | GUI 输出框自动填名维持旧样式 |

实现：`stage_path`/`lang_code` 助手落 `srt_io.py`（语言映射表 + 文件名安全化回退）；`PipelineConfig.bilingual` 字段 + CLI `--no-bilingual`；README 输出说明重写；ws 技能速查同步。

验证：3 例命名单测；真实冒烟四发——keep 双开关三文件命名正确、翻译默认双语双终稿并存（original 与终稿时间轴实测不同，覆盖病根除的直接证据）、`--no-bilingual` 单语、单步 translate 默认 `mini_chs.srt`。

## 测试基建（29e20c4）

`tests/` 从零建立：conftest + 8 个测试文件，本轮结束共 **47 例全绿（~3s）**。设计原则全离线可重复——假 openai、桩模型、临时配置路径、离屏 Qt，绝不触碰真实用户配置。含 2 条现状契约测试（翻译取消传播），T07 真修时会失败提醒更新断言。

pytest 注意：清华源对新 wheel 持续 403，pytest 经官方源+Clash 临时装入 .venv，**未入锁**（入锁需整体重解析会撞 demucs 403 且连累 WritanSub.bat 自动 sync）。`uv sync` 会清掉它，重装：`uv pip install pytest --index-url https://pypi.org/simple`（挂代理）。镜像恢复后 `uv add --group dev pytest` 转正 + 移除 hatchling<1.31 钉。

## 对齐中沉淀的决策与知识

- **取消延迟地图**（按流水线序）：解码=无效（T03 未修，GIL 冻结）→ 降噪=12s 窗口前向（T21 起）→ whisper=当前段秒级（≥v0.1.5.1）→ 对齐=当前条 → 翻译=当前 API 调用。用户旧体感"模型必须跑完整个视频"对降噪成立至 T21 修复前，对解码至今成立。
- **取消体验三件套已排入批次 4 与 T03 同批**（tracker 已记）：解码取消随 T03 痊愈；翻译 API 弃单；可选 torch forward-pre-hook 毫秒级取消。线程模型下"任意时刻即时安全取消"不存在，唯一真即时是进程隔离+杀进程——**关窗强杀是用户有意保留的正确设计**，T36 只补确认框且排 T03 之后（用户上次改 T36 痛苦的根源即 T03 的 GIL 冻结）。
- T29 可行性共识：对齐不需要读音完美，只需占住大致音长；数字+量词是 MeCab 词典覆盖最好的场景；A/B 兜底不达标不入库。
- T22 本质：两模型打分公式不同（MMS=token 后验均值，Qwen3=时长比），共用 0.5 阈值必有一个失效；修法是分模型标定默认值。

## 遗留

- 批次 3（重构批）：review 数据流组 T06+T10+T34（**建议优先**，T01 后错位已用户可见）；内存/解码组 T17+T28（模型释放/波形驻留，与 T20 遗留合并）
- 批次 4（架构批）：T03 native 替换 + 取消三件套 + T30 存储布局 + T16/T37 收尾
- 等素材：T29→T22；等触发：T09/T12/T13/T36 及池底项
- 本报告与 Audit/Fix/Review 三份同样保持未跟踪，入库与否用户定
