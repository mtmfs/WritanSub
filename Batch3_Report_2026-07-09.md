# WritanSub 批次 3 报告 — review 数据流重构 + separate 重叠双模式 + T28 半项与回退

- 日期：2026-07-09（同日先序见 `Review_Report` / `Batch2_Report`）
- 范围：批次 3 重排后两块（review 组 T06+T10+T34、T28），含一次做后回退
- 本轮 3 个 commit，全部已推送：

| Commit | 内容 |
|--------|------|
| `dc9c90c` | T06/T10/T34 review 数据流重构 + separate 重叠双模式 |
| `2d1792f` | T28 无预处理解码去重 + compute_type 参数化 |
| `3fa0062` | T28 消重解半项回退（保留 compute_type） |

批次 3 重排记录：T17（模型释放/波形驻留）经用户判**暂缓**移出——生产未暴露、验证成本高收益低；T20 复核遗留并入 T17 待回头。

---

## review 组（T06 + T10 + T34）

核心设计：低置信标记落 `Sub.low_words` 随本体携带，对齐（`replace` 保字段）、参考映射、短字幕合并三处变换自然跟随；全流程索引稳定后 `generate_review_final` 单点生成 review 文件；`mark_low_align_in_review`（回补已写盘文件的模式，含 `rfind(",,")` 脆弱解析）整体删除。设计代理预审抓掉两个致命坑：`run_alignment` 重建 Sub 会静默丢新字段（改 `replace`）；`replace` 浅拷贝共享 `low_words` 引用（禁 `+=`，一律重绑）。

T10 双模式（用户新增需求）：cue 打 `speaker` 标签，重叠处理移入 post_process——**默认 merge** 把跨说话人重叠压成一条 `- 甲` / `- 乙`（时间并集、score 取 min、分离串扰致两轨同句时自动去重）；`--overlap-mode keep` 保留双条真实重叠（gap 循环跳过异说话人重叠对，压平元凶 `curr.end = nxt.start` 对其失效）。vad_filter 补传全轨转录（区域短块故意不开防误吞）；词级数据 (sub, word) 成对携带过丢弃/排序/重编号——separate 模式的词级 review 结构性复活。

T34 随批：短字幕合并加"原始间距 ≤ gap_threshold"守卫，孤立短句不再被跨静音拽进前句；拉丁分隔符半项经对齐判不做（纯 CJK 场景）。

**验证**：新增 30 例单测（遗留行为零回归/双模式全分支/浅拷贝守卫/字段携带链）；真实冒烟三发——review 与终稿 20 块对 20 块时间轴逐块一致（行级 12、词级 15 标注落位正确）；separate 检出 22 段重叠（34.3%），merge 产出双说话人不同台词 cue（`- ギリギリで来る` / `- ギリギリだか`），keep 模式 22 对重叠全部存活。

**行为变化知会**：review 文件改在流程末尾产出（旧：转录阶段就写一版）；取消时不产出，与终稿同性质。无标记时清理陈旧 review 文件。

## T28：半项落地，半项做后回退

**保留**：`--compute-type {int8,int8_float16,float16}`（pipeline/transcribe），默认 int8 行为不变；whisper 缓存键带量化档防同进程切档复用旧模型。float16 实跑验证可用。

**回退**：消重复解码。完整经过——探查发现 tracker"解码 2–3 次"的真实范围只剩**无预处理场景**（--denoise 本已只解一次），按原授权实现（预解码 16k 临时 wav 两阶段共用）并验证可用后，用户质疑点醒：其工作流恒开降噪（本日冒烟亦证明裸跑全是音乐幻觉），该场景**零收益**，且预解码改变 whisper 输入前端会使转录结果变化（实测病理样本从整段"初音ミク"幻觉变为真实台词——虽属改善，但为不存在的场景引入行为差异不可取）。回退后无预处理输出与 raw 基线**逐字节一致**。`load_wav`（stdlib wave 读取，torchaudio 2.10 的 load 需 torchcodec 故不可用）保留为工具函数。

**教训（tracker 已记）**：探查后范围缩水到目标场景消失时，应回头重新拍板而非按原授权继续。

## 新立项：T41 wav 内存缓存模式（待对齐）

用户需求：同一媒体分多次命令处理时，解码后的 wav 存内存复用，生存期到进程被 kill 或显式清空。已入 tracker P3 表；形态对齐用户定回头再谈，届时从 tracker 条目接续。**待对齐的前提问题**：CLI 分开敲的多条命令是多个独立进程，内存不跨进程共享，目标形态需三选一——a) GUI 同进程多页复用（registry 加波形缓存，最小改）；b) CLI 常驻/守护形态（大改，接近 T03 的架构层）；c) 落盘缓存（跨进程可用，"内存"换"磁盘"，与已保留的 load_wav 天然衔接）。另注意与 T17 减驻留方向相反，须显式开关默认关。

## 测试与状态

- 套件累计 **82 例全绿（~3s）**：批次 1.5 基建 38 + T24 3 + T01 3 + T04 3 + 批次 3 新增 35
- tracker：T06/T10/T34 → `[x]`，T28 → `[~]`（参数化done/消重解判不做），T41 新增 `[ ]`
- 剩余池：批次 4（T03 + 取消三件套 + T30）、暂缓 T17、等素材 T29→T22、T41 待对齐、零散低优先项
